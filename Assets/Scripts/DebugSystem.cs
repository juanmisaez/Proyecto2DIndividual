using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DebugSystem : MonoBehaviour
{
    public static bool debugMode = false;
    public static bool debugInfiniteOxygen = true; // submodo que no permita la reducción del oxigeno
    public static bool debugBreakEverything = true; // submodo que permita romper todos los bloques desde un inicio
}